server {
    listen 80;
    server_name katala-jonni.com;
    location / {
        proxy_pass http://library:3000;
    }
    location /counter {
        proxy_pass http://counter:3001;
        rewrite ^/(.*) /$1 break;
    }
}
